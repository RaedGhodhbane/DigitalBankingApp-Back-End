#spring.datasource.url=jdbc:h2:mem:bank
#spring.h2.console.enabled=true
server.port=8085
spring.datasource.url=jdbc:mysql://localhost:3306/ebank?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
#avec create les donnÃ©es sauvegardÃ©es dans la base de donnÃ©es seront Ã©crasÃ©es
spring.jpa.hibernate.ddl-auto=update
#create pour la première deploiement de l'application ( premier usage )
#update pour faire la mise à jour ( structure et données ), on le fait aprés le create
#none : On utilise none lorsqu'on veut pas changer la structure de la base de données qui est déjà
#créé dans le SGBD (MySQL ou bien Oracle...)
#Lorsque la base de données ne contient aucune table, le mappage JPA hibernate ne passe pas ->
#C'est pourquoi, aucune table ne sera créé dans le SGBD (MySQL ou bien Oracle...)
## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MariaDBDialect
spring.jpa.show-sql=true